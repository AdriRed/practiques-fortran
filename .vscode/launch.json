{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gfortran) Iniciar",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/builds/${fileBasenameNoExtension}.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}/builds",
            "environment": [],
            "externalConsole": false,
            "preLaunchTask": "compile-fortran",
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Habilitar la impresión con sangría para gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": false
                }
            ]
        }
    ]
}